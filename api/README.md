## Available Scripts

In the project directory, you can run:
### `npm start`
Starting the server
### `docker:build`
Builds the docker image for the client
### `docker:start`
starts the docker container and is available on [Localhost:3000](http://localhost:3000/)
